---
import TldrLayout from "~/layouts/TldrLayout.astro";

const paths = Astro.url.pathname
  .split("/")
  .filter((p) => p !== "" && p !== "tools");
const category = paths[0];
const name = paths[1].replace("-", " ");
---

<TldrLayout>
  <slot />
</TldrLayout>
