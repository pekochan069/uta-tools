---
import { TbChevronRight } from "solid-icons/tb";

import WithSidebar from "./WithSidebar.astro";

type Props = {
  category: string;
  tool: string;
};

const { category, tool }: Props = Astro.props;

const capitalize = (str: string) => {
  return str
    .split("-")
    .map((s) => s[0].toUpperCase() + s.substring(1))
    .join(" ");
};
---

<WithSidebar title={capitalize(tool)}>
  <main class="py-4">
    <div class="mb-4 flex items-center gap-x-1 text-sm text-muted-foreground">
      <h2 class="capitalize">
        {category}
      </h2>
      <TbChevronRight class="inline-block h-4 w-4" />
      <h3 class="font-semibold capitalize">
        {tool}
      </h3>
    </div>
    <slot />
  </main>
</WithSidebar>
